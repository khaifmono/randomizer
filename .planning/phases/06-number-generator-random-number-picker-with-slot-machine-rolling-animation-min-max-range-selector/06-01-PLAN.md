---
phase: 06-number-generator
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/randomizer/use-number.ts
  - apps/web/src/lib/randomizer/use-number.test.ts
  - apps/web/src/lib/randomizer/types.ts
  - apps/web/src/index.css
autonomous: true
requirements: []
must_haves:
  truths:
    - "useNumber generates a random integer within the configured min/max range"
    - "useNumber exposes rolling state that guards against double-trigger"
    - "useNumber computes the correct digit array for slot-machine reel display"
    - "useNumber creates history entries in newest-first order with correct label format"
    - "Range presets (1-10, 1-100, 1-1000) and custom min/max are supported"
    - "CSS reel animation scrolls digits vertically with staggered stop timing"
  artifacts:
    - path: "apps/web/src/lib/randomizer/use-number.ts"
      provides: "Number generator hook with range, rolling, digits, history"
      exports: ["useNumber", "REEL_STAGGER_MS", "BASE_REEL_DURATION_MS"]
    - path: "apps/web/src/lib/randomizer/use-number.test.ts"
      provides: "TDD tests for useNumber hook"
      contains: "describe.*useNumber"
    - path: "apps/web/src/lib/randomizer/types.ts"
      provides: "Updated TabId type including number"
      contains: "number"
    - path: "apps/web/src/index.css"
      provides: "CSS keyframes for number reel scrolling animation"
      contains: "@keyframes number-reel-scroll"
  key_links:
    - from: "apps/web/src/lib/randomizer/use-number.ts"
      to: "apps/web/src/lib/randomizer/types.ts"
      via: "import HistoryEntry"
      pattern: "import.*HistoryEntry.*from.*types"
---

<objective>
Create the useNumber hook with TDD and the CSS slot-machine reel animation classes.

Purpose: The hook encapsulates all number generation logic (random number within range, digit decomposition for reel display, animation state, history tracking) following the exact pattern established by useDice and useCoin. The CSS provides the vertical digit-scrolling keyframes for the slot-machine reel effect.

Output: Fully tested useNumber hook and CSS animation classes in index.css.
</objective>

<execution_context>
@/Users/khaif/Documents/code-repo/base-project/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khaif/Documents/code-repo/base-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-number-generator-random-number-picker-with-slot-machine-rolling-animation-min-max-range-selector/06-CONTEXT.md

<interfaces>
<!-- Existing types and patterns the executor needs -->

From apps/web/src/lib/randomizer/types.ts:
```typescript
type HistoryEntry = {
  id: number;
  label: string;
  timestamp: number;
};

type TabId = "wheel" | "dice" | "coin";

export type { HistoryEntry, TabId };
```

From apps/web/src/lib/randomizer/use-dice.ts (pattern to follow):
```typescript
import { useState, useRef, useCallback } from "react";
import type { HistoryEntry } from "./types";

const ANIMATION_DURATION = 1200;

function useDice() {
  const [count, setCountState] = useState(2);
  const [rolling, setRolling] = useState(false);
  const [results, setResults] = useState<number[]>([]);
  const [sum, setSum] = useState<number | null>(null);
  const [history, setHistory] = useState<HistoryEntry[]>([]);

  const isRollingRef = useRef(false);
  const pendingResultsRef = useRef<number[]>([]);
  const nextIdRef = useRef(1);
  // ... setCount, startRoll, onRollEnd callbacks
}

export { useDice, ANIMATION_DURATION };
```

CSS animation pattern from index.css (dice example):
```css
@keyframes dice-roll {
  0%   { transform: translateY(0) rotateX(0deg) rotateY(0deg); }
  50%  { transform: translateY(-140px) rotateX(540deg) rotateY(360deg); }
  100% { transform: translateY(0) rotateX(720deg) rotateY(720deg); }
}

.dice-cube.dice-rolling {
  animation: dice-roll 1.2s cubic-bezier(0.2, 0, 0.3, 1) forwards;
}
```

Accent token pattern:
```css
--color-wheel-accent: oklch(0.55 0.18 240);
--color-dice-accent: oklch(0.55 0.15 145);
--color-coin-accent: oklch(0.72 0.15 80);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useNumber hook with TDD tests</name>
  <files>
    apps/web/src/lib/randomizer/use-number.test.ts,
    apps/web/src/lib/randomizer/use-number.ts,
    apps/web/src/lib/randomizer/types.ts
  </files>
  <read_first>
    apps/web/src/lib/randomizer/use-dice.ts,
    apps/web/src/lib/randomizer/use-dice.test.ts,
    apps/web/src/lib/randomizer/use-coin.ts,
    apps/web/src/lib/randomizer/types.ts
  </read_first>
  <behavior>
    - Test 1: Initial state — min=1, max=100 (per D-05), rolling=false, result=null, digits=[], history=[]
    - Test 2: setRange(1, 10) updates min to 1 and max to 10
    - Test 3: setRange with min > max is a no-op
    - Test 4: setRange with min < 0 or max > 999999 is a no-op (reasonable upper bound)
    - Test 5: setRange is a no-op while rolling
    - Test 6: startRoll sets rolling=true and pre-determines a result within [min, max]
    - Test 7: startRoll does nothing when already rolling (double-trigger guard via isRollingRef)
    - Test 8: result value is always >= min and <= max (test with mocked Math.random)
    - Test 9: digits array correctly decomposes result into individual digit values — e.g., 42 becomes [4, 2], 7 becomes [7], 100 becomes [1, 0, 0]
    - Test 10: onRollEnd creates history entry with label format "42 (1-100)" showing result and range, clears rolling after 200ms settle delay
    - Test 11: history entries are prepended (newest first) with incrementing IDs
    - Test 12: Preset ranges work — calling setRange(1, 10), setRange(1, 100), setRange(1, 1000) sets min/max correctly
  </behavior>
  <action>
    **RED phase:** Write use-number.test.ts with all 12 tests. Follow the exact test structure from use-dice.test.ts — import from vitest, use renderHook/act from @testing-library/react, use vi.useFakeTimers for the settle delay test. Run tests, confirm all fail.

    **GREEN phase:** Create use-number.ts following the useDice/useCoin hook pattern exactly:

    ```typescript
    // Stagger timing per D-03: left digit locks first, each subsequent ~200ms later
    const REEL_STAGGER_MS = 200;
    // Base animation duration for the first (leftmost) reel
    const BASE_REEL_DURATION_MS = 1000;

    function useNumber() {
      const [min, setMinState] = useState(1);       // per D-05
      const [max, setMaxState] = useState(100);      // per D-05
      const [rolling, setRolling] = useState(false);
      const [result, setResult] = useState<number | null>(null);
      const [digits, setDigits] = useState<number[]>([]);
      const [history, setHistory] = useState<HistoryEntry[]>([]);

      const isRollingRef = useRef(false);
      const pendingResultRef = useRef<number | null>(null);
      const pendingDigitsRef = useRef<number[]>([]);
      const nextIdRef = useRef(1);

      const setRange = useCallback((newMin: number, newMax: number) => {
        if (isRollingRef.current) return;
        if (newMin > newMax) return;
        if (newMin < 0 || newMax > 999999) return;
        setMinState(newMin);
        setMaxState(newMax);
      }, []);

      const startRoll = useCallback(() => {
        if (isRollingRef.current) return;
        isRollingRef.current = true;
        // Pre-determine result before animation (same pattern as dice/coin)
        const value = Math.floor(Math.random() * (max - min + 1)) + min;
        const digitArray = String(value).split("").map(Number);
        pendingResultRef.current = value;
        pendingDigitsRef.current = digitArray;
        setResult(value);
        setDigits(digitArray);
        setRolling(true);
      }, [min, max]);

      const onRollEnd = useCallback(() => {
        const value = pendingResultRef.current;
        // Label format: "42 (1-100)" per D-07 showing range context
        const label = `${value} (${min}-${max})`;
        const entry: HistoryEntry = {
          id: nextIdRef.current++,
          label,
          timestamp: Date.now(),
        };
        setHistory((prev) => [entry, ...prev]);

        setTimeout(() => {
          isRollingRef.current = false;
          setRolling(false);
        }, 200);
      }, [min, max]);

      return { min, max, rolling, result, digits, history, setRange, startRoll, onRollEnd };
    }

    export { useNumber, REEL_STAGGER_MS, BASE_REEL_DURATION_MS };
    ```

    Note: onRollEnd includes min/max in its useCallback deps because the label needs current range values.

    Also update types.ts to add "number" to TabId:
    ```typescript
    type TabId = "wheel" | "dice" | "coin" | "number";
    ```

    **REFACTOR phase:** Clean up if needed. Run all tests to confirm green.
  </action>
  <verify>
    <automated>cd /Users/khaif/Documents/code-repo/randomizer-toolkit && pnpm --filter web exec vitest run src/lib/randomizer/use-number.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useNumber" apps/web/src/lib/randomizer/use-number.ts
    - grep -q "REEL_STAGGER_MS" apps/web/src/lib/randomizer/use-number.ts
    - grep -q "BASE_REEL_DURATION_MS" apps/web/src/lib/randomizer/use-number.ts
    - grep -q "isRollingRef" apps/web/src/lib/randomizer/use-number.ts
    - grep -q "pendingResultRef" apps/web/src/lib/randomizer/use-number.ts
    - grep -q '"number"' apps/web/src/lib/randomizer/types.ts
    - grep -q "describe.*useNumber" apps/web/src/lib/randomizer/use-number.test.ts
  </acceptance_criteria>
  <done>All 12 tests pass. useNumber hook exports useNumber, REEL_STAGGER_MS, BASE_REEL_DURATION_MS. TabId type includes "number". Hook follows the exact same pattern as useDice/useCoin with isRollingRef guard, pre-determined result, pendingResultRef, history with newest-first prepend, and 200ms settle delay.</done>
</task>

<task type="auto">
  <name>Task 2: CSS slot-machine reel animation and number accent token</name>
  <files>apps/web/src/index.css</files>
  <read_first>apps/web/src/index.css</read_first>
  <action>
    Add to the `:root` / theme section a new accent color for the number tool:
    ```css
    --color-number-accent: oklch(0.60 0.20 310);
    ```
    This is a purple/magenta hue (310 on oklch hue wheel), distinct from wheel (blue 240), dice (green 145), and coin (gold 80).

    Add a new CSS section after the "Coin flipper" section, before the "Wheel empty state confetti" section:

    ```css
    /* Number generator — slot-machine reels per D-01, D-02, D-03 */

    .number-reel {
      height: 60px;
      overflow: hidden;
      position: relative;
      width: 48px;
      border: 2px solid var(--border);
      border-radius: 8px;
      background: var(--card);
    }

    .number-reel-strip {
      display: flex;
      flex-direction: column;
      transition: transform 0.1s;
      will-change: transform;
    }

    .number-reel-digit {
      height: 60px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2rem;
      font-weight: 700;
      font-variant-numeric: tabular-nums;
      color: var(--foreground);
    }

    @keyframes number-reel-scroll {
      0%   { transform: translateY(0); }
      80%  { transform: translateY(calc(var(--reel-target) - 60px)); }
      92%  { transform: translateY(calc(var(--reel-target) + 10px)); }
      100% { transform: translateY(var(--reel-target)); }
    }

    .number-reel-strip.reel-spinning {
      animation: number-reel-scroll var(--reel-duration) cubic-bezier(0.2, 0, 0.1, 1) forwards;
    }

    .number-reel-strip.reel-stopped {
      transform: translateY(var(--reel-target));
    }
    ```

    **Design rationale per decisions:**
    - Per D-01: Individual digit columns (`.number-reel`) spin independently
    - Per D-02: Each reel scrolls through digits 0-9 vertically via CSS translateY
    - Per D-03: Staggered stop timing is handled via `--reel-duration` CSS variable, set per-reel in JS (leftmost reel gets BASE_REEL_DURATION_MS, each subsequent gets +REEL_STAGGER_MS)
    - The keyframe overshoots slightly at 92% (bounces past target by 10px then settles) for a satisfying mechanical feel
    - `--reel-target` is a CSS custom property set inline by the component to position at the correct digit
  </action>
  <verify>
    <automated>cd /Users/khaif/Documents/code-repo/randomizer-toolkit && grep -c "number-reel" apps/web/src/index.css</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "number-accent" apps/web/src/index.css
    - grep -q "@keyframes number-reel-scroll" apps/web/src/index.css
    - grep -q "reel-spinning" apps/web/src/index.css
    - grep -q "reel-stopped" apps/web/src/index.css
    - grep -q "number-reel-digit" apps/web/src/index.css
    - grep -q "reel-target" apps/web/src/index.css
    - grep -q "reel-duration" apps/web/src/index.css
  </acceptance_criteria>
  <done>index.css has --color-number-accent token, .number-reel container, .number-reel-strip with reel-spinning and reel-stopped classes, .number-reel-digit styling, @keyframes number-reel-scroll with overshoot bounce, CSS custom properties --reel-target and --reel-duration for per-reel JS control.</done>
</task>

</tasks>

<verification>
1. `cd /Users/khaif/Documents/code-repo/randomizer-toolkit && pnpm --filter web exec vitest run src/lib/randomizer/use-number.test.ts` — all 12 tests pass
2. `grep -q "number" apps/web/src/lib/randomizer/types.ts` — TabId includes "number"
3. `grep -q "@keyframes number-reel-scroll" apps/web/src/index.css` — CSS animation exists
4. `cd /Users/khaif/Documents/code-repo/randomizer-toolkit && pnpm --filter web exec vitest run src/lib/randomizer/` — all existing randomizer tests still pass (no regressions)
</verification>

<success_criteria>
- useNumber hook passes all TDD tests
- Hook exports useNumber, REEL_STAGGER_MS, BASE_REEL_DURATION_MS
- TabId type updated to include "number"
- CSS reel animation keyframes and classes added to index.css
- --color-number-accent token defined
- All existing dice/coin/wheel tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-number-generator-random-number-picker-with-slot-machine-rolling-animation-min-max-range-selector/06-01-SUMMARY.md`
</output>
